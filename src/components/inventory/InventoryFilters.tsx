"use client";
import { Search } from "lucide-react"; import type { InventoryStatus } from "@/types/inventory";
export default function InventoryFilters({ query, warehouse, status, warehouses, onQuery, onWarehouse, onStatus }: { query: string; warehouse: string; status: "all" | InventoryStatus; warehouses: string[]; onQuery: (value: string) => void; onWarehouse: (value: string) => void; onStatus: (value: "all" | InventoryStatus) => void }) { return <div className="flex flex-col gap-3 border-b border-slate-100 p-4 dark:border-slate-800 lg:flex-row"><div className="relative w-full lg:max-w-sm"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={18} /><input value={query} onChange={(e) => onQuery(e.target.value)} placeholder="Search stock..." className="h-10 w-full rounded-xl border border-slate-200 bg-slate-50 pl-10 pr-3 text-sm outline-none focus:border-emerald-500 dark:border-slate-700 dark:bg-slate-800" /></div><select value={warehouse} onChange={(e) => onWarehouse(e.target.value)} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-800"><option value="all">All warehouses</option>{warehouses.map((value) => <option key={value}>{value}</option>)}</select><select value={status} onChange={(e) => onStatus(e.target.value as "all" | InventoryStatus)} className="h-10 rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-800"><option value="all">All stock statuses</option><option value="in-stock">In stock</option><option value="low-stock">Low stock</option><option value="out-of-stock">Out of stock</option></select></div>; }
