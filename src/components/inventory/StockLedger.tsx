import { ArrowDownLeft, ArrowUpRight, Download, Printer } from "lucide-react"; import { formatDate } from "@/lib/utils"; import type { InventoryItem, StockLedgerEntry } from "@/types/inventory";
const labels: Record<StockLedgerEntry["type"], string> = { opening: "Opening", purchase: "Purchase", sale: "Sale", adjustment: "Adjustment", "transfer-in": "Transfer in", "transfer-out": "Transfer out", "production-in": "Production", "production-out": "Production" };
export default function StockLedger({ item, entries }: { item: InventoryItem; entries: StockLedgerEntry[] }) { const stockIn = entries.reduce((sum, entry) => sum + entry.stockIn, 0); const stockOut = entries.reduce((sum, entry) => sum + entry.stockOut, 0); return <div className="space-y-6"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:justify-between"><div><p className="text-sm text-slate-500">Stock ledger</p><h2 className="mt-1 text-xl font-bold">{item.productName}</h2><p className="mt-1 text-sm text-slate-500">{item.riceCode} · {item.warehouseName}</p></div><div className="flex gap-2"><button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold dark:border-slate-700"><Printer size={16} />Print</button><button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-xl bg-emerald-600 px-3 py-2 text-sm font-semibold text-white"><Download size={16} />Export</button></div></div></section><div className="grid gap-4 sm:grid-cols-3"><Stat label="Current stock" value={`${item.currentStock} ${item.unit}`} /><Stat label="Total stock in" value={`${stockIn} ${item.unit}`} positive /><Stat label="Total stock out" value={`${stockOut} ${item.unit}`} /></div><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800/40"><tr><th className="px-6 py-3">Date</th><th className="px-4 py-3">Description</th><th className="px-4 py-3">Reference</th><th className="px-4 py-3">Type</th><th className="px-4 py-3 text-right">Stock in</th><th className="px-4 py-3 text-right">Stock out</th><th className="px-6 py-3 text-right">Running balance</th></tr></thead><tbody>{entries.map((entry) => <tr key={entry.id} className="border-t border-slate-100 dark:border-slate-800"><td className="px-6 py-4 text-slate-500">{formatDate(entry.date)}</td><td className="px-4 py-4 font-medium">{entry.description}</td><td className="px-4 py-4 font-mono text-xs text-slate-500">{entry.reference}</td><td className="px-4 py-4"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600 dark:bg-slate-800">{labels[entry.type]}</span></td><td className="px-4 py-4 text-right font-semibold text-emerald-600">{entry.stockIn ? <span className="inline-flex items-center gap-1"><ArrowDownLeft size={14} />{entry.stockIn}</span> : "—"}</td><td className="px-4 py-4 text-right font-semibold text-rose-600">{entry.stockOut ? <span className="inline-flex items-center gap-1"><ArrowUpRight size={14} />{entry.stockOut}</span> : "—"}</td><td className="px-6 py-4 text-right font-bold">{entry.balance} {item.unit}</td></tr>)}</tbody></table></div></section></div>; }
function Stat({ label, value, positive = false }: { label: string; value: string; positive?: boolean }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm text-slate-500">{label}</p><p className={`mt-2 text-xl font-bold ${positive ? "text-emerald-600" : ""}`}>{value}</p></article>; }
