"use client";
import { X } from "lucide-react"; import { useState } from "react"; import type { InventoryItem, StockAdjustmentValues } from "@/types/inventory";
const initial: StockAdjustmentValues = { quantity: "", adjustmentType: "increase", reason: "Stock count correction", notes: "" };
export default function StockAdjustmentDialog({ item, open, onClose, onSave }: { item: InventoryItem | null; open: boolean; onClose: () => void; onSave: (values: StockAdjustmentValues) => void }) { const [values, setValues] = useState(initial); if (!open || !item) return null; function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (Number(values.quantity) > 0) { onSave(values); setValues(initial); } } return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4" role="dialog" aria-modal="true"><form onSubmit={submit} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl dark:bg-slate-900"><div className="flex justify-between"><div><h2 className="text-lg font-bold">Stock adjustment</h2><p className="mt-1 text-sm text-slate-500">{item.productName} · {item.warehouseName}</p></div><button type="button" onClick={onClose} className="rounded-lg p-1 text-slate-400 hover:bg-slate-100" aria-label="Close"><X size={20} /></button></div><div className="mt-6 grid gap-5"><label><span className="mb-2 block text-sm font-medium">Adjustment type</span><select value={values.adjustmentType} onChange={(e) => setValues((current) => ({ ...current, adjustmentType: e.target.value as StockAdjustmentValues["adjustmentType"] }))} className="h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-800"><option value="increase">Increase stock</option><option value="decrease">Decrease stock</option></select></label><label><span className="mb-2 block text-sm font-medium">Quantity</span><input required min="1" type="number" value={values.quantity} onChange={(e) => setValues((current) => ({ ...current, quantity: e.target.value }))} className="h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-emerald-500 dark:border-slate-700 dark:bg-slate-800" /></label><label><span className="mb-2 block text-sm font-medium">Reason</span><select value={values.reason} onChange={(e) => setValues((current) => ({ ...current, reason: e.target.value }))} className="h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-800"><option>Stock count correction</option><option>Damaged stock</option><option>Expired stock</option><option>Other</option></select></label><label><span className="mb-2 block text-sm font-medium">Notes</span><textarea rows={3} value={values.notes} onChange={(e) => setValues((current) => ({ ...current, notes: e.target.value }))} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-800" /></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold dark:border-slate-700">Cancel</button><button className="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white">Save adjustment</button></div></form></div>; }
