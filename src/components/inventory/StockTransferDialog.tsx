"use client";
import { X } from "lucide-react"; import { useState } from "react"; import type { InventoryItem, StockTransferValues } from "@/types/inventory";
const initial: StockTransferValues = { destinationWarehouseId: "", quantity: "", notes: "" };
export default function StockTransferDialog({ item, warehouses, open, onClose, onSave }: { item: InventoryItem | null; warehouses: Array<{ id: string; name: string }>; open: boolean; onClose: () => void; onSave: (values: StockTransferValues) => void }) { const [values, setValues] = useState(initial); if (!open || !item) return null; const availableStock = item.availableStock; const sourceWarehouseId = item.warehouseId; const unit = item.unit; function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (values.destinationWarehouseId && Number(values.quantity) > 0 && Number(values.quantity) <= availableStock) { onSave(values); setValues(initial); } } return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4" role="dialog" aria-modal="true"><form onSubmit={submit} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl dark:bg-slate-900"><div className="flex justify-between"><div><h2 className="text-lg font-bold">Transfer stock</h2><p className="mt-1 text-sm text-slate-500">Available: {availableStock} {unit}</p></div><button type="button" onClick={onClose} className="rounded-lg p-1 text-slate-400 hover:bg-slate-100" aria-label="Close"><X size={20} /></button></div><div className="mt-6 grid gap-5"><label><span className="mb-2 block text-sm font-medium">Destination warehouse</span><select required value={values.destinationWarehouseId} onChange={(e) => setValues((current) => ({ ...current, destinationWarehouseId: e.target.value }))} className="h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm dark:border-slate-700 dark:bg-slate-800"><option value="">Select warehouse</option>{warehouses.filter((warehouse) => warehouse.id !== sourceWarehouseId).map((warehouse) => <option key={warehouse.id} value={warehouse.id}>{warehouse.name}</option>)}</select></label><label><span className="mb-2 block text-sm font-medium">Quantity</span><input required min="1" max={availableStock} type="number" value={values.quantity} onChange={(e) => setValues((current) => ({ ...current, quantity: e.target.value }))} className="h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-emerald-500 dark:border-slate-700 dark:bg-slate-800" /></label><label><span className="mb-2 block text-sm font-medium">Notes</span><textarea rows={3} value={values.notes} onChange={(e) => setValues((current) => ({ ...current, notes: e.target.value }))} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm dark:border-slate-700 dark:bg-slate-800" /></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold dark:border-slate-700">Cancel</button><button className="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white">Create transfer</button></div></form></div>; }
