import { cn } from "@/lib/utils"; import type { InventoryStatus } from "@/types/inventory";
export default function InventoryStatusBadge({ status }: { status: InventoryStatus }) { const labels: Record<InventoryStatus, string> = { "in-stock": "In stock", "low-stock": "Low stock", "out-of-stock": "Out of stock" }; return <span className={cn("rounded-full px-2.5 py-1 text-xs font-semibold", status === "in-stock" ? "bg-emerald-50 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-400" : status === "low-stock" ? "bg-amber-50 text-amber-700 dark:bg-amber-500/10 dark:text-amber-400" : "bg-rose-50 text-rose-700 dark:bg-rose-500/10 dark:text-rose-400")}>{labels[status]}</span>; }
