import { Boxes, Warehouse } from "lucide-react"; import Link from "next/link"; import { formatCurrency } from "@/lib/utils"; import type { Product } from "@/types/product"; import ProductStatusBadge from "./ProductStatusBadge";
export default function ProductCard({ product }: { product: Product }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex justify-between gap-3"><div><Link href={`/products/view/${product.id}`} className="font-semibold hover:text-emerald-600">{product.productName}</Link><p className="mt-1 font-mono text-xs text-slate-500">{product.riceCode}</p></div><ProductStatusBadge status={product.status} /></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><p className="text-slate-500">Suggested price</p><p className="mt-1 font-bold">{formatCurrency(product.suggestedSalePrice)}</p></div><div><p className="text-slate-500">Stock</p><p className={`mt-1 font-bold ${product.currentStock <= product.minimumStock ? "text-rose-600" : ""}`}>{product.currentStock} {product.unit}</p></div></div><div className="mt-4 flex items-center justify-between text-sm text-slate-500"><span className="flex items-center gap-1.5"><Boxes size={15} />{product.category}</span><span className="flex items-center gap-1.5"><Warehouse size={15} />{product.warehouseCount} warehouses</span></div><Link href={`/products/view/${product.id}`} className="mt-5 block rounded-xl border border-slate-200 py-2 text-center text-sm font-semibold text-emerald-600 dark:border-slate-700">View product</Link></article>; }
