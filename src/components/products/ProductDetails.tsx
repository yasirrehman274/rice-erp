import { Boxes, Package } from "lucide-react"; import { formatCurrency, formatDate } from "@/lib/utils"; import type { Product, ProductMovement } from "@/types/product"; import ProductStatusBadge from "./ProductStatusBadge";
export default function ProductDetails({ product, purchases, sales }: { product: Product; purchases: ProductMovement[]; sales: ProductMovement[] }) { return <div className="space-y-6"><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_360px]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 sm:p-6"><div className="flex gap-4"><span className="grid size-16 shrink-0 place-items-center rounded-2xl bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15"><Package size={28} /></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-bold">{product.productName}</h2><ProductStatusBadge status={product.status} /></div><p className="mt-1 font-mono text-sm text-slate-500">{product.riceCode} · Created {formatDate(product.createdDate)}</p><div className="mt-5 grid gap-3 text-sm text-slate-500 sm:grid-cols-2"><p>Category: <strong className="text-slate-800 dark:text-slate-100">{product.category}</strong></p><p>Brand: <strong className="text-slate-800 dark:text-slate-100">{product.brand}</strong></p><p>Variety: <strong className="text-slate-800 dark:text-slate-100">{product.variety}</strong></p><p>Packaging: <strong className="text-slate-800 dark:text-slate-100">{product.bagWeight} / {product.unit}</strong></p></div></div></div><div className="mt-6 border-t border-slate-100 pt-5 dark:border-slate-800"><h3 className="text-sm font-semibold">Description</h3><p className="mt-2 text-sm leading-6 text-slate-500">{product.description}</p></div></section><aside className="rounded-2xl bg-emerald-600 p-6 text-white shadow-lg shadow-emerald-600/20"><div className="flex justify-between"><p className="text-sm text-emerald-100">Current stock</p><Boxes size={22} className="text-emerald-200" /></div><p className="mt-3 text-3xl font-bold">{product.currentStock} {product.unit}</p><p className="mt-2 text-sm text-emerald-100">Minimum level: {product.minimumStock} {product.unit}</p><div className="mt-7 border-t border-white/15 pt-4 text-sm"><p className="flex justify-between"><span>Warehouses</span><strong>{product.warehouseCount}</strong></p></div></aside></div><div className="grid gap-4 sm:grid-cols-3"><Stat label="Purchase price" value={formatCurrency(product.purchasePrice)} /><Stat label="Sale price" value={formatCurrency(product.salePrice)} /><Stat label="Profit per unit" value={formatCurrency(product.salePrice - product.purchasePrice)} /></div><div className="grid gap-6 xl:grid-cols-2"><MovementTable title="Recent purchases" movements={purchases} /><MovementTable title="Recent sales" movements={sales} /></div></div>; }
function Stat({ label, value }: { label: string; value: string }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-xl font-bold">{value}</p></article>; }
function MovementTable({ title, movements }: { title: string; movements: ProductMovement[] }) { return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="p-5"><h2 className="font-semibold">{title}</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[500px] text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-xs uppercase text-slate-500 dark:border-slate-800 dark:bg-slate-800/40"><tr><th className="px-5 py-3">Reference</th><th className="px-3 py-3">Party</th><th className="px-3 py-3">Quantity</th><th className="px-5 py-3 text-right">Amount</th></tr></thead><tbody>{movements.map((item) => <tr key={item.id} className="border-b border-slate-100 last:border-0 dark:border-slate-800"><td className="px-5 py-4 font-mono text-xs">{item.reference}</td><td className="px-3 py-4">{item.party}</td><td className="px-3 py-4">{item.quantity}</td><td className="px-5 py-4 text-right font-semibold">{formatCurrency(item.amount)}</td></tr>)}</tbody></table></div></section>; }
