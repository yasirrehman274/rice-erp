import { Boxes, MapPin, UserRound } from "lucide-react"; import Link from "next/link"; import type { Warehouse } from "@/types/warehouse"; import WarehouseStatusBadge from "./WarehouseStatusBadge";
export default function WarehouseCard({ warehouse }: { warehouse: Warehouse }) { const percentage = warehouse.capacity ? Math.round((warehouse.occupiedCapacity / warehouse.capacity) * 100) : 0; return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex justify-between gap-3"><div><Link href={`/warehouses/view/${warehouse.id}`} className="font-semibold hover:text-emerald-600">{warehouse.name}</Link><p className="mt-1 font-mono text-xs text-slate-500">{warehouse.code}</p></div><WarehouseStatusBadge status={warehouse.status} /></div><div className="mt-5 space-y-2.5 text-sm text-slate-500"><p className="flex items-center gap-2"><UserRound size={15} />{warehouse.manager}</p><p className="flex items-center gap-2"><MapPin size={15} />{warehouse.city}</p><p className="flex items-center gap-2"><Boxes size={15} />{warehouse.totalStock.toLocaleString()} bags · {warehouse.productCount} products</p></div><div className="mt-5"><div className="mb-2 flex justify-between text-xs"><span className="text-slate-500">Capacity used</span><strong>{percentage}%</strong></div><div className="h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className={`h-full rounded-full ${percentage >= 90 ? "bg-rose-500" : percentage >= 70 ? "bg-amber-500" : "bg-emerald-500"}`} style={{ width: `${percentage}%` }} /></div></div><Link href={`/warehouses/view/${warehouse.id}`} className="mt-5 block rounded-xl border border-slate-200 py-2 text-center text-sm font-semibold text-emerald-600 dark:border-slate-700">View warehouse</Link></article>; }
