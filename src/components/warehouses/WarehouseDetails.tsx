import { Boxes, MapPin, Phone, UserRound, Warehouse as WarehouseIcon } from "lucide-react"; import { formatDate } from "@/lib/utils"; import type { Warehouse, WarehouseStockItem } from "@/types/warehouse"; import WarehouseStatusBadge from "./WarehouseStatusBadge";
export default function WarehouseDetails({ warehouse, stock }: { warehouse: Warehouse; stock: WarehouseStockItem[] }) { const percentage = warehouse.capacity ? Math.round((warehouse.occupiedCapacity / warehouse.capacity) * 100) : 0; return <div className="space-y-6"><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_360px]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 sm:p-6"><div className="flex gap-4"><span className="grid size-16 shrink-0 place-items-center rounded-2xl bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15"><WarehouseIcon size={28} /></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-bold">{warehouse.name}</h2><WarehouseStatusBadge status={warehouse.status} /></div><p className="mt-1 font-mono text-sm text-slate-500">{warehouse.code} · Created {formatDate(warehouse.createdDate)}</p><div className="mt-5 grid gap-3 text-sm text-slate-500 sm:grid-cols-2"><p className="flex items-center gap-2"><UserRound size={16} />{warehouse.manager}</p><p className="flex items-center gap-2"><Phone size={16} />{warehouse.phone}</p><p className="flex items-center gap-2"><MapPin size={16} />{warehouse.city}</p><p className="truncate">{warehouse.email}</p></div></div></div><div className="mt-6 border-t border-slate-100 pt-5 dark:border-slate-800"><h3 className="text-sm font-semibold">Address</h3><p className="mt-2 text-sm leading-6 text-slate-500">{warehouse.address}</p><h3 className="mt-5 text-sm font-semibold">Notes</h3><p className="mt-2 text-sm leading-6 text-slate-500">{warehouse.notes}</p></div></section><aside className="rounded-2xl bg-emerald-600 p-6 text-white shadow-lg shadow-emerald-600/20"><div className="flex justify-between"><p className="text-sm text-emerald-100">Current stock</p><Boxes size={22} className="text-emerald-200" /></div><p className="mt-3 text-3xl font-bold">{warehouse.totalStock.toLocaleString()} bags</p><p className="mt-2 text-sm text-emerald-100">{warehouse.productCount} active products</p><div className="mt-7 border-t border-white/15 pt-4"><div className="flex justify-between text-sm"><span>Capacity used</span><strong>{percentage}%</strong></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/20"><div className="h-full rounded-full bg-white" style={{ width: `${percentage}%` }} /></div><p className="mt-3 text-xs text-emerald-100">{warehouse.occupiedCapacity.toLocaleString()} of {warehouse.capacity.toLocaleString()} bags</p></div></aside></div><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="p-5 sm:p-6"><h2 className="font-semibold">Warehouse stock</h2><p className="mt-1 text-sm text-slate-500">Product quantities currently stored at this location.</p></div>{stock.length ? <div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:border-slate-800 dark:bg-slate-800/40"><tr><th className="px-6 py-3">Product</th><th className="px-4 py-3">Rice code</th><th className="px-4 py-3 text-right">Quantity</th><th className="px-6 py-3 text-right">Minimum stock</th></tr></thead><tbody>{stock.map((item) => <tr key={item.id} className="border-b border-slate-100 last:border-0 dark:border-slate-800"><td className="px-6 py-4 font-medium">{item.product}</td><td className="px-4 py-4 font-mono text-xs text-slate-500">{item.riceCode}</td><td className={`px-4 py-4 text-right font-semibold ${item.quantity <= item.minimumStock ? "text-rose-600" : ""}`}>{item.quantity.toLocaleString()} {item.unit}</td><td className="px-6 py-4 text-right">{item.minimumStock} {item.unit}</td></tr>)}</tbody></table></div> : <div className="p-10 text-center text-sm text-slate-500">No stock is currently assigned to this warehouse.</div>}</section></div>; }
