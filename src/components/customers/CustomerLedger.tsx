"use client";
import { Download, Printer } from "lucide-react"; import { formatCurrency, formatDate } from "@/lib/utils"; import type { Customer, CustomerLedgerEntry } from "@/types/customer";
export default function CustomerLedger({ customer, entries }: { customer: Customer; entries: CustomerLedgerEntry[] }) { const debit = entries.reduce((sum, item) => sum + item.debit, 0); const credit = entries.reduce((sum, item) => sum + item.credit, 0); const closing = entries.at(-1)?.balance ?? customer.openingBalance; return <div className="space-y-6"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:justify-between"><div><p className="text-sm text-slate-500">Customer ledger</p><h2 className="mt-1 text-xl font-bold">{customer.name}</h2><p className="mt-1 text-sm text-slate-500">{customer.businessName} · {customer.phone}</p></div><div className="flex gap-2"><button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold dark:border-slate-700"><Printer size={16} />Print</button><button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-xl bg-emerald-600 px-3 py-2 text-sm font-semibold text-white"><Download size={16} />Export</button></div></div></section><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card label="Opening balance" value={customer.openingBalance} /><Card label="Total debit" value={debit} /><Card label="Total credit" value={credit} /><Card label="Closing balance" value={closing} strong /></div><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800/40"><tr><th className="px-6 py-3">Date</th><th className="px-4 py-3">Description</th><th className="px-4 py-3">Reference</th><th className="px-4 py-3 text-right">Debit</th><th className="px-4 py-3 text-right">Credit</th><th className="px-6 py-3 text-right">Running balance</th></tr></thead><tbody>{entries.map((item) => <tr key={item.id} className="border-t border-slate-100 dark:border-slate-800"><td className="px-6 py-4 text-slate-500">{formatDate(item.date)}</td><td className="px-4 py-4 font-medium">{item.description}</td><td className="px-4 py-4 font-mono text-xs text-slate-500">{item.reference}</td><td className="px-4 py-4 text-right">{item.debit ? formatCurrency(item.debit) : "—"}</td><td className="px-4 py-4 text-right">{item.credit ? formatCurrency(item.credit) : "—"}</td><td className="px-6 py-4 text-right font-bold">{formatCurrency(item.balance)}</td></tr>)}</tbody><tfoot className="border-t-2 border-slate-200 bg-slate-50 font-semibold dark:border-slate-700 dark:bg-slate-800/40"><tr><td colSpan={3} className="px-6 py-4">Totals</td><td className="px-4 py-4 text-right">{formatCurrency(debit)}</td><td className="px-4 py-4 text-right">{formatCurrency(credit)}</td><td className="px-6 py-4 text-right">{formatCurrency(closing)}</td></tr></tfoot></table></div></section></div>; }
function Card({ label, value, strong = false }: { label: string; value: number; strong?: boolean }) { return <article className={`rounded-2xl border p-5 shadow-sm ${strong ? "border-emerald-600 bg-emerald-600 text-white" : "border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"}`}><p className={`text-sm ${strong ? "text-emerald-100" : "text-slate-500"}`}>{label}</p><p className="mt-2 text-xl font-bold">{formatCurrency(value)}</p></article>; }
