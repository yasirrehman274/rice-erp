import { Building2, MapPin, Phone } from "lucide-react"; import Link from "next/link"; import { formatCurrency } from "@/lib/utils"; import type { Customer } from "@/types/customer";
export default function CustomerCard({ customer }: { customer: Customer }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-start gap-3"><span className="grid size-11 place-items-center rounded-xl bg-emerald-100 text-sm font-bold text-emerald-700 dark:bg-emerald-500/15">{customer.name.slice(0, 2).toUpperCase()}</span><div className="min-w-0 flex-1"><Link href={`/customers/view/${customer.id}`} className="block truncate font-semibold hover:text-emerald-600">{customer.name}</Link><p className="mt-1 truncate text-sm text-slate-500">{customer.businessName}</p></div><span className={`rounded-full px-2 py-1 text-xs font-semibold ${customer.status === "active" ? "bg-emerald-50 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-400" : "bg-slate-100 text-slate-600 dark:bg-slate-800"}`}>{customer.status}</span></div><div className="mt-5 space-y-2.5 text-sm text-slate-500"><p className="flex items-center gap-2"><Phone size={15} />{customer.phone}</p><p className="flex items-center gap-2"><MapPin size={15} />{customer.city}</p><p className="flex items-center gap-2"><Building2 size={15} />Balance: <strong className="text-slate-800 dark:text-slate-100">{formatCurrency(customer.currentBalance)}</strong></p></div><Link href={`/customers/view/${customer.id}`} className="mt-5 block rounded-xl border border-slate-200 py-2 text-center text-sm font-semibold text-emerald-600 dark:border-slate-700">View customer</Link></article>; }
