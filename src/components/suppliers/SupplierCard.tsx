import { Building2, MapPin, Phone } from "lucide-react";
import Link from "next/link";
import { formatCurrency } from "@/lib/utils";
import type { Supplier } from "@/types/supplier";

export default function SupplierCard({ supplier }: { supplier: Supplier }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-start gap-3"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-emerald-100 text-sm font-bold text-emerald-700 dark:bg-emerald-500/15">{supplier.name.slice(0, 2).toUpperCase()}</span><div className="min-w-0 flex-1"><Link href={`/suppliers/view/${supplier.id}`} className="block truncate font-semibold hover:text-emerald-600">{supplier.name}</Link><p className="mt-1 text-sm text-slate-500">{supplier.contactPerson}</p></div><span className={`rounded-full px-2 py-1 text-xs font-semibold ${supplier.status === "active" ? "bg-emerald-50 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-400" : "bg-slate-100 text-slate-600 dark:bg-slate-800 dark:text-slate-400"}`}>{supplier.status}</span></div><div className="mt-5 space-y-2.5 text-sm text-slate-500"><p className="flex items-center gap-2"><Phone size={15} />{supplier.phone}</p><p className="flex items-center gap-2"><MapPin size={15} />{supplier.city}</p><p className="flex items-center gap-2"><Building2 size={15} />Balance: <span className="font-semibold text-slate-800 dark:text-slate-100">{formatCurrency(supplier.currentBalance)}</span></p></div><Link href={`/suppliers/view/${supplier.id}`} className="mt-5 block rounded-xl border border-slate-200 py-2 text-center text-sm font-semibold text-emerald-600 hover:bg-emerald-50 dark:border-slate-700 dark:hover:bg-emerald-500/10">View supplier</Link></article>; }
