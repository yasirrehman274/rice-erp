import { Building2, FileText, MapPin, Phone, WalletCards } from "lucide-react";
import Link from "next/link";
import { formatCurrency, formatDate } from "@/lib/utils";
import type { Supplier, SupplierPurchase } from "@/types/supplier";

export default function SupplierDetails({ supplier, purchases }: { supplier: Supplier; purchases: SupplierPurchase[] }) { return <div className="space-y-6"><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_360px]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-start"><span className="grid size-16 shrink-0 place-items-center rounded-2xl bg-emerald-100 text-xl font-bold text-emerald-700 dark:bg-emerald-500/15">{supplier.name.slice(0, 2).toUpperCase()}</span><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-bold">{supplier.name}</h2><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${supplier.status === "active" ? "bg-emerald-50 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-400" : "bg-slate-100 text-slate-600 dark:bg-slate-800"}`}>{supplier.status}</span></div><p className="mt-1 text-sm text-slate-500">Supplier since {formatDate(supplier.createdAt)}</p><div className="mt-5 grid gap-3 text-sm text-slate-600 sm:grid-cols-2 dark:text-slate-400"><p className="flex items-center gap-2"><Phone size={16} />{supplier.phone}</p><p className="flex items-center gap-2"><Building2 size={16} />{supplier.contactPerson}</p><p className="flex items-center gap-2"><MapPin size={16} />{supplier.city}</p><p className="flex items-center gap-2"><FileText size={16} />{supplier.email}</p></div></div></div><div className="mt-6 border-t border-slate-100 pt-5 dark:border-slate-800"><h3 className="text-sm font-semibold">Business address</h3><p className="mt-2 text-sm leading-6 text-slate-500">{supplier.address}</p>{supplier.notes && <><h3 className="mt-5 text-sm font-semibold">Notes</h3><p className="mt-2 text-sm leading-6 text-slate-500">{supplier.notes}</p></>}</div></section><aside className="rounded-2xl bg-emerald-600 p-6 text-white shadow-lg shadow-emerald-600/20"><div className="flex items-center justify-between"><p className="text-sm text-emerald-100">Current balance</p><WalletCards size={22} className="text-emerald-200" /></div><p className="mt-3 text-3xl font-bold">{formatCurrency(supplier.currentBalance)}</p><p className="mt-2 text-sm text-emerald-100">Payable to this supplier</p><Link href={`/suppliers/ledger/${supplier.id}`} className="mt-7 block rounded-xl bg-white/15 px-4 py-2.5 text-center text-sm font-semibold hover:bg-white/25">View full ledger</Link></aside></div><section className="grid gap-4 sm:grid-cols-3"><Summary label="Total purchases" value={formatCurrency(supplier.totalPurchases)} /><Summary label="Total paid" value={formatCurrency(supplier.totalPaid)} /><Summary label="Credit limit" value={formatCurrency(supplier.creditLimit)} /></section><section className="rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center justify-between p-5 sm:p-6"><div><h2 className="font-semibold">Recent purchases</h2><p className="mt-1 text-sm text-slate-500">Latest purchase orders from this supplier</p></div><Link href={`/suppliers/ledger/${supplier.id}`} className="text-sm font-semibold text-emerald-600">View ledger</Link></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:border-slate-800 dark:bg-slate-800/40"><tr><th className="px-6 py-3">Reference</th><th className="px-4 py-3">Date</th><th className="px-4 py-3">Product</th><th className="px-4 py-3">Quantity</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{purchases.map((purchase) => <tr key={purchase.id} className="border-b border-slate-100 last:border-0 dark:border-slate-800"><td className="px-6 py-4 font-mono text-xs font-semibold">{purchase.id}</td><td className="px-4 py-4 text-slate-500">{formatDate(purchase.date)}</td><td className="px-4 py-4">{purchase.product}</td><td className="px-4 py-4">{purchase.quantity}</td><td className="px-4 py-4 font-semibold">{formatCurrency(purchase.amount)}</td><td className="px-4 py-4"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600 dark:bg-slate-800">{purchase.status}</span></td></tr>)}</tbody></table></div></section></div>; }
function Summary({ label, value }: { label: string; value: string }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-xl font-bold">{value}</p></article>; }
