import ProductPageActions from "@/components/products/ProductPageActions"; import ProductTable from "@/components/products/ProductTable"; import { products } from "@/data/products";
export default function ProductsPage() { return <div className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-emerald-600">Inventory catalog</p><h1 className="mt-1 text-2xl font-bold tracking-tight sm:text-3xl">Rice products</h1><p className="mt-1 text-sm text-slate-500">Manage product catalog, pricing, and inventory thresholds.</p></div><ProductPageActions products={products} /></div><div className="grid gap-4 sm:grid-cols-3"><Stat label="Total products" value={String(products.length)} /><Stat label="Active products" value={String(products.filter((item) => item.status === "active").length)} /><Stat label="Low stock products" value={String(products.filter((item) => item.currentStock <= item.minimumStock).length)} /></div><ProductTable initialProducts={products} /></div>; }
function Stat({ label, value }: { label: string; value: string }) { return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-xl font-bold">{value}</p></article>; }
