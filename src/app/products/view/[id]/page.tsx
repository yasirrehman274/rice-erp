import { ChevronLeft, Pencil } from "lucide-react"; import Link from "next/link"; import { notFound } from "next/navigation"; import ProductDetails from "@/components/products/ProductDetails"; import { getProductById, getProductMovements } from "@/data/products";
export default async function ViewProductPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const product = getProductById(id); if (!product) notFound(); const movements = getProductMovements(product); return <div><div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><Link href="/products" className="inline-flex items-center gap-1 text-sm font-semibold text-slate-500 hover:text-emerald-600"><ChevronLeft size={17} />Back to products</Link><h1 className="mt-3 text-2xl font-bold tracking-tight sm:text-3xl">Product details</h1></div><Link href={`/products/edit/${id}`} className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white"><Pencil size={16} />Edit product</Link></div><ProductDetails product={product} purchases={movements.purchases} sales={movements.sales} /></div>; }
