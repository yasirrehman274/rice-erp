import { ChevronLeft, Pencil } from "lucide-react"; import Link from "next/link"; import { notFound } from "next/navigation"; import WarehouseDetails from "@/components/warehouses/WarehouseDetails"; import { getWarehouseById, getWarehouseStock } from "@/data/warehouses";
export default async function ViewWarehousePage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const warehouse = getWarehouseById(id); if (!warehouse) notFound(); return <div><div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><Link href="/warehouses" className="inline-flex items-center gap-1 text-sm font-semibold text-slate-500 hover:text-emerald-600"><ChevronLeft size={17} />Back to warehouses</Link><h1 className="mt-3 text-2xl font-bold tracking-tight sm:text-3xl">Warehouse details</h1></div><Link href={`/warehouses/edit/${id}`} className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white"><Pencil size={16} />Edit warehouse</Link></div><WarehouseDetails warehouse={warehouse} stock={getWarehouseStock(warehouse)} /></div>; }
