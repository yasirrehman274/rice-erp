import SupplierPageActions from "@/components/suppliers/SupplierPageActions";
import SupplierTable from "@/components/suppliers/SupplierTable";
import { suppliers } from "@/data/suppliers";

export default function SuppliersPage() { return <div className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-emerald-600">Supplier management</p><h1 className="mt-1 text-2xl font-bold tracking-tight sm:text-3xl">Suppliers</h1><p className="mt-1 text-sm text-slate-500">Manage supplier profiles, balances, and purchase records.</p></div><SupplierPageActions suppliers={suppliers} /></div><div className="grid gap-4 sm:grid-cols-3"><MiniStat label="Total suppliers" value={String(suppliers.length)} /><MiniStat label="Active suppliers" value={String(suppliers.filter((supplier) => supplier.status === "active").length)} /><MiniStat label="Total payable" value={`Rs. ${new Intl.NumberFormat("en-PK").format(suppliers.reduce((sum, supplier) => sum + supplier.currentBalance, 0))}`} /></div><SupplierTable initialSuppliers={suppliers} /></div>; }
function MiniStat({ label, value }: { label: string; value: string }) { return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-xl font-bold">{value}</p></article>; }
