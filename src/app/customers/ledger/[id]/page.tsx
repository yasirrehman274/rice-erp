import { ChevronLeft } from "lucide-react"; import Link from "next/link"; import { notFound } from "next/navigation"; import CustomerLedger from "@/components/customers/CustomerLedger"; import { getCustomerById, getCustomerLedger } from "@/data/customers";
export default async function CustomerLedgerPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const customer = getCustomerById(id); if (!customer) notFound(); return <div><Link href={`/customers/view/${id}`} className="mb-4 inline-flex items-center gap-1 text-sm font-semibold text-slate-500 hover:text-emerald-600"><ChevronLeft size={17} />Back to customer profile</Link><div className="mb-6"><h1 className="text-2xl font-bold tracking-tight sm:text-3xl">Customer ledger</h1><p className="mt-1 text-sm text-slate-500">Review sales, payments, and outstanding balance.</p></div><CustomerLedger customer={customer} entries={getCustomerLedger(customer)} /></div>; }
