import { ChevronLeft } from "lucide-react"; import Link from "next/link"; import { notFound } from "next/navigation"; import StockLedger from "@/components/inventory/StockLedger"; import { getInventoryItem, getStockLedger } from "@/data/inventory";
export default async function InventoryLedgerPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const item = getInventoryItem(id); if (!item) notFound(); return <div><Link href="/inventory" className="mb-4 inline-flex items-center gap-1 text-sm font-semibold text-slate-500 hover:text-emerald-600"><ChevronLeft size={17} />Back to inventory</Link><div className="mb-6"><h1 className="text-2xl font-bold tracking-tight sm:text-3xl">Stock ledger</h1><p className="mt-1 text-sm text-slate-500">Review all stock movements for this product and warehouse.</p></div><StockLedger item={item} entries={getStockLedger(item)} /></div>; }
